
if [ $SUDAH != 3 ];
then 
SUDAH=$((SUDAH+=1));
     echo "memulai pembuatan codenvy nomer $SUDAH";
     echo $SUDAH;
     #xvfb-run -a node sampan.js
else
SUDAH=0
echo "tidur 1 jam"
sleep 3600
#xvfb-run -a node sampan.js
fi;